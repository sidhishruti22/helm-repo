apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-charts.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
